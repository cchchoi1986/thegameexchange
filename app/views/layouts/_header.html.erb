<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a id="custom-logo" class="navbar-brand" href="#">The Game Exchange</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if params[:controller] == 'static_pages' %>"><a href="/">Home</a></li>
        <li class="<%= 'active' if params[:controller] == 'users' %>">
        <%= link_to "User", "users/#{current_user.id}" %></li>
        <li><a href="#contact">Contact</a></li>
        
        <li id="userprofile">
          <li><%= link_to current_user.username, "users/#{current_user.id}", :id => 'username' %>
            <% if current_user %>
                  <!-- default :method is :POST for button_to -->
              <span>
                <%= link_to "Edit Profile", edit_user_registration_path, :method => :get, :id => 'profile-options' %>
                <%= link_to "Sign out", destroy_user_session_path, :method =>  :delete, :id => 'profile-options' %>
              </span>
            <% else %>
              <span>
                <%= link_to "Sign up", new_user_registration_path, :id => 'profile-options' %>
                <%= button_to "Sign in", new_user_session_path, :id => 'profile-options' %>
              </span>
            <% end %>
          </li>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>

</nav>