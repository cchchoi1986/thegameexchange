<nav id="header" class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed ccfont-black font-black" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only ccfont-black font-black">Toggle navigation</span>
        <span class="icon-bar ccfont-black font-black"></span>
        <span class="icon-bar ccfont-black font-black"></span>
        <span class="icon-bar ccfont-black font-black"></span>
      </button>
      <a id="custom-logo" class="navbar-brand" href="/">The GaME ExCHANGE</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right ccfont-black font-black">
        <li class="<%= 'active' if params[:controller] == 'static_pages' %> ccfont-black font-black">
          <%= link_to " Home", "/", :id => "cc-home", :class => "glyphicon glyphicon-home ccfont-black font-black" %>
        </li>
        <li class="dropdown <%= 'active' if params[:controller] == 'users' %>">
          <a href="#" id="cc-profile" class="dropdown-toggle glyphicon glyphicon-user ccfont-black font-black" data-toggle="dropdown" role="button" aria-expanded="false">
            <% if current_user %>
              <%= current_user.username %>
            <% else %>
              Login
            <% end %>

          <span class="caret"></span></a>
          <ul class="dropdown-menu cc-font-black font-black" role="menu">
            <% if current_user %>
                  <!-- default :method is :POST for button_to -->
                <li><%= link_to "My Profile", "/users", :class => "font-black" %></li>
                <li class="divider"></li>
                <li><%= link_to "Edit Profile", edit_user_registration_path, :method => :get, :id => 'profile-options', :class => "ccfont-black font-black" %></li>
                <li><%= link_to "Sign out", destroy_user_session_path, :method =>  :delete, :id => 'profile-options', :class => "ccfont-black font-black" %></li>
            <% else %>
                <li><%= link_to "Register", new_user_registration_path, :id => 'profile-options', :class => "font-black" %></li>
                <li><%= link_to "Login", new_user_session_path, :id => 'profile-options', :class => "ccfont-black font-black" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>

</nav>