<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a id="custom-logo" class="navbar-brand" href="/">The Game Exchange</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if params[:controller] == 'static_pages' %>">
          <%= link_to "Home", "/" %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">
            <% if current_user %>
              <%= current_user.username %>
            <% else %>
              Sign Up / In
            <% end %>

          <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% if current_user %>
                  <!-- default :method is :POST for button_to -->
                <li><%= link_to "My Profile", "users" %></li>
                <li class="divider"></li>
                <li><%= link_to "Edit Profile", edit_user_registration_path, :method => :get, :id => 'profile-options' %></li>
                <li><%= link_to "Sign out", destroy_user_session_path, :method =>  :delete, :id => 'profile-options' %></li>
            <% else %>
                <li><%= link_to "Register", new_user_registration_path, :id => 'profile-options' %></li>
                <li><%= link_to "Login", new_user_session_path, :id => 'profile-options' %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>

</nav>